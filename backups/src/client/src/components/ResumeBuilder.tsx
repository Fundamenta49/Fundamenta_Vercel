// This is a duplicate of resume-builder.tsx (lowercase)
// We're keeping it for backward compatibility

import { default as ResumeBuilderComponent } from "./resume-builder";

export default function ResumeBuilder() {
  return <ResumeBuilderComponent />;
}